#lang racket

(define garbage (read-line))
(let ((a (cons (map string->number (string-split (read-line)))))
      (b (cons (map string->number (string-split (read-line))))))
  (let* ((x1 0)
         (x2 0)
         (hi (first (sort x2))))
    ))
